//! Keyboard input handling — see [`HexViewState::handle_key`] in `lib.rs`.
//! This module is intentionally empty; all input logic lives in `lib.rs`
//! next to the state it mutates.  The module is kept as a placeholder for
//! future search / goto-offset dialog logic.
